import * as React from "react";
import { Card, CardContent, CardHeader } from '@material-ui/core';

export default () => (
  <Card>
    <CardHeader
      title="Dashboard"
    />
    <CardContent>
      Manage the users, send notifications and more...
    </CardContent>
  </Card>
);
